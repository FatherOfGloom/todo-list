add long-only option parsing
fix the read/write shuffling
add done marking with mark or m option followed by an index
add numeration to a printed list
add a config struct with filename and todo list list :ÐÐD
save that option is done. Marker notation?
add insertion
add indented insertion
mb add first line as list opcodes
add a working list of list names embedded into a parsable config
add data subfolder. Handle IO
add binary serialization
if no file is set - usage
?add marked option sorting
